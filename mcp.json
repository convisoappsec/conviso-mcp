{
  "mcpServers": {
    "conviso": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@convisoappsec/mcp@0.7.0"
      ],
      "env": {
        "CONVISO_API_KEY": "${CONVISO_API_KEY}"
      }
    }
  }
}
