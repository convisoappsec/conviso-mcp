{
  "mcpServers": {
    "conviso": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@convisoappsec/mcp@latest"
      ],
      "env": {
        "CONVISO_API_KEY": "${CONVISO_API_KEY}"
      }
    }
  }
}
